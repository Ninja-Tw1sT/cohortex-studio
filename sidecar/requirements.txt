# Cohortex Studio sidecar — thin FastAPI wrapper around the Cohortex agent framework.
#
# Local dev: cohortex is installed in editable mode from the sibling repo:
#   pip install -e ../../cohortex
# Production (Cloud Run): pin to a specific commit so deploys are reproducible —
# update this SHA when you want to pick up newer Cohortex changes.
cohortex @ git+https://github.com/Ninja-Tw1sT/cohortex.git@a6c1e551514afdfab6a0b1c7d51850dda11d9bdd

fastapi~=0.139
uvicorn[standard]~=0.49
pydantic~=2.13
mcp~=1.28
httpx~=0.28
python-dotenv~=1.2

# Dev/test only
pytest~=8.3
